langcode: en
status: true
dependencies:
  config:
    - user.role.external_user
  module:
    - user
id: user_add_role_action.external_user
label: 'Add the External User role to the selected user(s)'
type: user
plugin: user_add_role_action
configuration:
  rid: external_user
